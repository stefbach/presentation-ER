/* Voix-off MUA — en attente de génération (assets/vo/mua_scene_<n>.mp3).
   Tant que window.VO est null, chaque scène utilise sa durée par défaut (s.dur).
   L'ancienne voix-off ER (fr_scene_*.mp3) ne correspond plus au script MUA. */
window.VO=null;
